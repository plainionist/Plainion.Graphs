# Plainion.Graphs

Simple library for graph analysis - the "swiss armi knife" of every software engineer 😎

## Build NuGet Package

```bash
dotnet pack src/Plainion.Graphs/Plainion.Graphs.csproj -c Release
```

The `.nupkg` file will be in `src/Plainion.Graphs/bin/Release/`.
